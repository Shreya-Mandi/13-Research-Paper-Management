API:

1. "http://localhost:6969/Login/"
    method: POST

    request:
    {
      usrName : String,
      usrPwd : String
    }

    response:
    {
      valid : true / false,
      type : "student" / "faculty"
    }


2. "http://localhost:6969/Register/"
    method: POST

    request:
    {
      usrName : String,
      usrPwd : String,
      type : "student" / "faculty"
    }

    response:
    {
      status : true / false
    }


3. "http://localhost:6969/GetProjectList/"
    method: POST

    request:
    {
      usrName : String,
      type : "student" / "faculty"
    }

    response:
    {
      ProjectList : [
                      {
                        ProjectID : String,
                        ProjectTitle : String,
                        ProjectStatus : String,
                        FacultyID : String,
                        FacultyName : String
                      }, ...
                    ]
    }
